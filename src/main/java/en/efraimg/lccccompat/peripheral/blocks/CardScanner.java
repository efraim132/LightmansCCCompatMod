package en.efraimg.lccccompat.peripheral.blocks;
import net.minecraft.world.level.block.Block;

public class CardScanner extends Block {
    public CardScanner(Properties properties) {
		super(properties);
    }
}
